name: Setup DNS

on:
  workflow_dispatch:

jobs:
  setup-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium
      
      - name: Add CNAME record
        run: |
          cat > setup_dns.js << 'SCRIPT'
          const { chromium } = require('playwright');

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage();
            
            try {
              console.log('🌐 Логинюсь...');
              await page.goto('https://hosting.timeweb.ru/login');
              await page.waitForTimeout(2000);
              await page.fill('input[name="username"]', 'cp02586');
              await page.fill('input[name="password"]', '6CNgRdxX222');
              await page.click('button[type="submit"]');
              await page.waitForTimeout(5000);
              console.log('✅ Вошли, URL:', page.url());
              
              // Навигация через меню: Домены
              console.log('🔍 Домены...');
              await page.click('text=Домены');
              await page.waitForTimeout(2000);
              
              // Мои домены
              await page.click('text=Мои домены');
              await page.waitForTimeout(3000);
              
              await page.screenshot({ path: '01_domains.png', fullPage: true });
              console.log('URL доменов:', page.url());
              
              // Ищем artvision.pro и кликаем
              console.log('🔍 artvision.pro...');
              await page.click('text=artvision.pro');
              await page.waitForTimeout(3000);
              
              await page.screenshot({ path: '02_domain_settings.png', fullPage: true });
              console.log('URL настроек:', page.url());
              
              // Переходим на вкладку DNS
              console.log('🔍 DNS...');
              await page.click('text=Редактор DNS');
              await page.waitForTimeout(3000);
              
              // Закрываем попапы
              for (let i = 0; i < 3; i++) {
                await page.keyboard.press('Escape');
                await page.waitForTimeout(300);
              }
              try { await page.click('button:has-text("Закрыть")', { timeout: 2000 }); } catch {}
              
              await page.screenshot({ path: '03_dns.png', fullPage: true });
              console.log('URL DNS:', page.url());
              
              // Проверяем portal
              const html = await page.content();
              if (html.includes('portal.artvision.pro') || html.includes('>portal<')) {
                console.log('⚠️ Запись portal уже есть!');
                await browser.close();
                return;
              }
              
              // Добавить запись
              console.log('➕ Добавить запись...');
              await page.click('button:has-text("Добавить запись")');
              await page.waitForTimeout(2000);
              
              await page.screenshot({ path: '04_form.png', fullPage: true });
              
              // Кликаем на dropdown типа (там "A")
              console.log('📝 Выбираю тип CNAME...');
              // Находим select внутри модального окна
              const modal = page.locator('text=Новая запись').locator('..');
              await modal.locator('select').first().selectOption('CNAME');
              await page.waitForTimeout(1000);
              
              await page.screenshot({ path: '05_cname.png', fullPage: true });
              
              // Заполняем хост = portal
              console.log('📝 Ввожу portal...');
              const hostInput = page.locator('input').first();
              await hostInput.fill('portal');
              await page.waitForTimeout(500);
              
              // Заполняем значение
              console.log('📝 Ввожу cname.vercel-dns.com...');
              // После выбора CNAME должно быть поле для домена
              const inputs = await page.locator('input:visible').all();
              for (let i = 0; i < inputs.length; i++) {
                const val = await inputs[i].inputValue();
                if (val === '' && i > 0) {
                  await inputs[i].fill('cname.vercel-dns.com');
                  console.log('✅ Заполнил значение в input #' + i);
                  break;
                }
              }
              
              await page.screenshot({ path: '06_filled.png', fullPage: true });
              
              // Сохраняем
              console.log('💾 Сохраняю...');
              await page.click('button:has-text("Сохранить")');
              await page.waitForTimeout(4000);
              
              await page.screenshot({ path: '07_result.png', fullPage: true });
              
              // Проверяем
              const result = await page.content();
              if (result.includes('portal')) {
                console.log('✅✅✅ УСПЕХ! Запись portal добавлена!');
              } else {
                console.log('❓ Проверь скриншот');
              }
              
            } catch (e) {
              console.error('❌', e.message);
              await page.screenshot({ path: 'error.png', fullPage: true });
            }
            
            await browser.close();
          })();
          SCRIPT
          
          node setup_dns.js
      
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: "*.png"
