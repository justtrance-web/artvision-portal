name: Setup DNS

on:
  workflow_dispatch:

jobs:
  setup-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium
      
      - name: Add CNAME record
        run: |
          cat > setup_dns.js << 'SCRIPT'
          const { chromium } = require('playwright');

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage();
            
            try {
              console.log('🌐 Открываю Timeweb...');
              await page.goto('https://hosting.timeweb.ru/login', { timeout: 60000 });
              await page.waitForTimeout(2000);
              
              console.log('🔑 Логинюсь...');
              await page.fill('input[name="username"]', 'cp02586');
              await page.fill('input[name="password"]', '6CNgRdxX222');
              await page.click('button[type="submit"]');
              await page.waitForTimeout(5000);
              
              console.log('🔍 Открываю DNS...');
              await page.goto('https://hosting.timeweb.ru/domains/settings/dns-editor?fqdn=artvision.pro');
              await page.waitForTimeout(3000);
              
              // Проверяем есть ли уже portal
              const content = await page.content();
              if (content.includes('portal.artvision.pro') || content.includes('portal\t') || content.includes('>portal<')) {
                console.log('✅ Запись portal УЖЕ существует!');
                await page.screenshot({ path: '01_already_exists.png', fullPage: true });
                await browser.close();
                return;
              }
              
              console.log('➕ Добавляю запись...');
              await page.click('text=Добавить запись');
              await page.waitForTimeout(2000);
              await page.screenshot({ path: '02_modal.png', fullPage: true });
              
              // Кликаем на dropdown типа (там где написано "A")
              console.log('📝 Выбираю тип CNAME...');
              
              // Находим select или dropdown с типом
              const typeDropdown = await page.$('select, [class*="select"], [class*="dropdown"]');
              if (typeDropdown) {
                // Если это select
                const tagName = await typeDropdown.evaluate(el => el.tagName);
                if (tagName === 'SELECT') {
                  await typeDropdown.selectOption('CNAME');
                } else {
                  // Кликаем чтобы открыть
                  await typeDropdown.click();
                  await page.waitForTimeout(500);
                  await page.click('text=CNAME');
                }
              } else {
                // Ищем кнопку/div с текстом "A" и кликаем
                const typeBtn = await page.$('button:has-text("A"), div:has-text("A")');
                if (typeBtn) {
                  await typeBtn.click();
                  await page.waitForTimeout(500);
                  // Ищем CNAME в выпадающем списке
                  await page.click('li:has-text("CNAME"), div:has-text("CNAME"), option:has-text("CNAME")');
                }
              }
              
              await page.waitForTimeout(1000);
              await page.screenshot({ path: '03_type_selected.png', fullPage: true });
              
              // Заполняем хост
              console.log('📝 Заполняю хост...');
              // Ищем input рядом с текстом "Хост" или с placeholder
              const hostInput = await page.$('input[placeholder*="Хост"], input[name*="host"], input[name*="name"]');
              if (hostInput) {
                await hostInput.fill('portal');
              } else {
                // Первый текстовый input в модалке
                const inputs = await page.$$('.modal input[type="text"], dialog input[type="text"]');
                if (inputs.length > 0) {
                  await inputs[0].fill('portal');
                }
              }
              
              await page.waitForTimeout(500);
              
              // Заполняем значение CNAME
              console.log('📝 Заполняю значение...');
              const valueInput = await page.$('input[placeholder*="значение"], input[placeholder*="value"], input[name*="value"], input[name*="target"], input[name*="content"]');
              if (valueInput) {
                await valueInput.fill('cname.vercel-dns.com');
              } else {
                // Второй input или input после смены типа
                const inputs = await page.$$('.modal input[type="text"]:visible, dialog input[type="text"]:visible');
                if (inputs.length > 1) {
                  await inputs[1].fill('cname.vercel-dns.com');
                }
              }
              
              await page.screenshot({ path: '04_filled.png', fullPage: true });
              
              // Сохраняем
              console.log('💾 Сохраняю...');
              await page.click('button:has-text("Сохранить")');
              await page.waitForTimeout(3000);
              await page.screenshot({ path: '05_result.png', fullPage: true });
              
              // Проверяем результат
              const newContent = await page.content();
              if (newContent.includes('portal')) {
                console.log('✅ УСПЕХ! Запись portal добавлена!');
              } else {
                console.log('⚠️ Проверь скриншоты');
              }
              
            } catch (e) {
              console.error('❌ Ошибка:', e.message);
              await page.screenshot({ path: 'error.png', fullPage: true });
            }
            
            await browser.close();
          })();
          SCRIPT
          
          node setup_dns.js
      
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: "*.png"
