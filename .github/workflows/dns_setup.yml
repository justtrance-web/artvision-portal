name: Setup DNS

on:
  workflow_dispatch:

jobs:
  setup-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium
      
      - name: Add CNAME record
        run: |
          cat > setup_dns.js << 'SCRIPT'
          const { chromium } = require('playwright');

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage();
            
            try {
              console.log('🌐 Открываю Timeweb...');
              await page.goto('https://hosting.timeweb.ru/login', { timeout: 60000 });
              await page.waitForTimeout(2000);
              
              console.log('🔑 Логинюсь...');
              await page.fill('input[name="username"]', 'cp02586');
              await page.fill('input[name="password"]', '6CNgRdxX222');
              await page.click('button[type="submit"]');
              await page.waitForTimeout(5000);
              
              console.log('🔍 Открываю DNS...');
              await page.goto('https://hosting.timeweb.ru/domains/settings/dns-editor?fqdn=artvision.pro');
              await page.waitForTimeout(3000);
              
              // Проверяем portal
              let content = await page.content();
              if (content.includes('portal.artvision.pro')) {
                console.log('✅ Запись portal УЖЕ существует!');
                await browser.close();
                return;
              }
              
              console.log('➕ Открываю форму...');
              await page.click('button:has-text("Добавить")');
              await page.waitForTimeout(2000);
              await page.screenshot({ path: '01_form.png', fullPage: true });
              
              // Кликаем на кнопку dropdown типа
              console.log('📝 Кликаю на dropdown типа...');
              await page.click('[class*="selectWidget"] button');
              await page.waitForTimeout(1000);
              await page.screenshot({ path: '02_dropdown_open.png', fullPage: true });
              
              // Выбираем CNAME из списка
              console.log('📝 Выбираю CNAME...');
              // Ищем элемент с текстом CNAME в выпадающем списке
              await page.click('[class*="option"]:has-text("CNAME"), li:has-text("CNAME"), [role="option"]:has-text("CNAME"), div:has-text("CNAME"):not([class*="select"])');
              await page.waitForTimeout(1000);
              await page.screenshot({ path: '03_cname_selected.png', fullPage: true });
              
              // Заполняем хост
              console.log('📝 Заполняю хост = portal...');
              // Поле хоста после смены типа
              const hostInput = await page.$('input[name*="ost"], input[name*="name"], input[placeholder*="Хост"]');
              if (hostInput) {
                await hostInput.fill('portal');
              } else {
                // Пробуем по индексу - первый видимый text input после hidden
                await page.fill('[class*="dialog"] input:not([type="hidden"]):visible >> nth=0', 'portal');
              }
              
              // Заполняем target
              console.log('📝 Заполняю target = cname.vercel-dns.com...');
              const targetInput = await page.$('input[name*="alue"], input[name*="arget"], input[name*="ontent"]');
              if (targetInput) {
                await targetInput.fill('cname.vercel-dns.com');
              } else {
                // Второй видимый input
                await page.fill('[class*="dialog"] input:not([type="hidden"]):visible >> nth=1', 'cname.vercel-dns.com');
              }
              
              await page.screenshot({ path: '04_filled.png', fullPage: true });
              
              // Сохраняем
              console.log('💾 Сохраняю...');
              await page.click('button:has-text("Сохранить")');
              await page.waitForTimeout(3000);
              await page.screenshot({ path: '05_result.png', fullPage: true });
              
              content = await page.content();
              if (content.includes('portal')) {
                console.log('✅ УСПЕХ! Запись portal добавлена!');
              } else {
                console.log('⚠️ Проверь скриншоты');
              }
              
            } catch (e) {
              console.error('❌ Ошибка:', e.message);
              await page.screenshot({ path: 'error.png', fullPage: true });
            }
            
            await browser.close();
          })();
          SCRIPT
          
          node setup_dns.js
      
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: "*.png"
