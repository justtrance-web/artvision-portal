name: Setup DNS

on:
  workflow_dispatch:

jobs:
  setup-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium
      
      - name: Add CNAME record
        run: |
          cat > setup_dns.js << 'SCRIPT'
          const { chromium } = require('playwright');

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage();
            
            try {
              console.log('🌐 Логинюсь...');
              await page.goto('https://hosting.timeweb.ru/login');
              await page.waitForTimeout(2000);
              await page.fill('input[name="username"]', 'cp02586');
              await page.fill('input[name="password"]', '6CNgRdxX222');
              await page.click('button[type="submit"]');
              await page.waitForTimeout(5000);
              console.log('✅ Вошли:', page.url());
              
              // Переходим на страницу доменов
              console.log('🔍 Открываю домены...');
              await page.goto('https://hosting.timeweb.ru/domains');
              await page.waitForTimeout(3000);
              
              await page.screenshot({ path: '01_domains.png', fullPage: true });
              
              // Ищем artvision.pro и кликаем
              console.log('🔍 Ищу artvision.pro...');
              await page.click('text=artvision.pro');
              await page.waitForTimeout(2000);
              
              await page.screenshot({ path: '02_domain_settings.png', fullPage: true });
              console.log('URL:', page.url());
              
              // Кликаем на вкладку "Редактор DNS"
              console.log('📝 Открываю Редактор DNS...');
              await page.click('text=Редактор DNS');
              await page.waitForTimeout(2000);
              
              await page.screenshot({ path: '03_dns_editor.png', fullPage: true });
              
              // Закрываем попапы
              for (let i = 0; i < 5; i++) {
                await page.keyboard.press('Escape');
                await page.waitForTimeout(200);
              }
              try {
                await page.click('button:has-text("Закрыть")', { timeout: 1000 });
              } catch {}
              
              await page.waitForTimeout(1000);
              
              // Проверяем portal
              const html = await page.content();
              if (html.includes('portal.artvision.pro') || html.match(/portal\s+CNAME/)) {
                console.log('⚠️ Запись portal уже есть!');
                await page.screenshot({ path: '04_portal_exists.png', fullPage: true });
                await browser.close();
                return;
              }
              
              // Ищем и кликаем "+ Добавить запись"
              console.log('➕ Добавить запись...');
              await page.click('text=Добавить запись');
              await page.waitForTimeout(2000);
              
              await page.screenshot({ path: '04_add_form.png', fullPage: true });
              
              // Кликаем на выпадающий список типа (сейчас показывает "A")
              console.log('📝 Выбираю тип CNAME...');
              // Кликаем на элемент с текстом "A" внутри модального окна
              const modal = page.locator('text=Новая запись').locator('..');
              const typeButton = modal.locator('text=A').first();
              await typeButton.click();
              await page.waitForTimeout(500);
              
              await page.screenshot({ path: '05_type_dropdown.png', fullPage: true });
              
              // Выбираем CNAME из списка
              await page.click('text=CNAME');
              await page.waitForTimeout(1000);
              
              await page.screenshot({ path: '06_cname_selected.png', fullPage: true });
              
              // Заполняем поле хоста (portal)
              console.log('📝 Заполняю portal...');
              // Поле ввода перед .artvision.pro
              const hostInput = page.locator('input').filter({ hasNot: page.locator('[type="hidden"]') }).first();
              await hostInput.fill('portal');
              await page.waitForTimeout(500);
              
              // Заполняем значение CNAME
              console.log('📝 Заполняю cname.vercel-dns.com...');
              const valueInput = page.locator('input').filter({ hasNot: page.locator('[type="hidden"]') }).nth(1);
              await valueInput.fill('cname.vercel-dns.com');
              await page.waitForTimeout(500);
              
              await page.screenshot({ path: '07_filled.png', fullPage: true });
              
              // Сохраняем
              console.log('💾 Сохраняю...');
              await page.click('button:has-text("Сохранить")');
              await page.waitForTimeout(4000);
              
              await page.screenshot({ path: '08_result.png', fullPage: true });
              
              // Проверяем результат
              const result = await page.content();
              if (result.includes('portal')) {
                console.log('✅✅✅ УСПЕХ! Запись portal добавлена!');
              }
              
            } catch (e) {
              console.error('❌', e.message);
              await page.screenshot({ path: 'error.png', fullPage: true });
            }
            
            await browser.close();
          })();
          SCRIPT
          
          node setup_dns.js
      
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: "*.png"
