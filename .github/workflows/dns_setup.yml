name: Setup DNS

on:
  workflow_dispatch:

jobs:
  setup-dns:
    runs-on: ubuntu-latest
    steps:
      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright
          npx playwright install chromium
      
      - name: Add CNAME record
        run: |
          cat > setup_dns.js << 'SCRIPT'
          const { chromium } = require('playwright');

          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage();
            
            try {
              console.log('🌐 Открываю Timeweb...');
              await page.goto('https://hosting.timeweb.ru/login', { timeout: 60000 });
              await page.waitForTimeout(2000);
              
              console.log('🔑 Логинюсь...');
              await page.fill('input[name="username"]', 'cp02586');
              await page.fill('input[name="password"]', '6CNgRdxX222');
              await page.click('button[type="submit"]');
              await page.waitForTimeout(5000);
              
              console.log('🔍 Открываю DNS...');
              await page.goto('https://hosting.timeweb.ru/domains/settings/dns-editor?fqdn=artvision.pro');
              await page.waitForTimeout(3000);
              
              // Проверяем есть ли уже portal
              const content = await page.content();
              if (content.includes('portal.artvision.pro')) {
                console.log('✅ Запись portal УЖЕ существует!');
                await page.screenshot({ path: 'already_exists.png', fullPage: true });
                await browser.close();
                return;
              }
              
              console.log('➕ Открываю форму добавления...');
              await page.click('button:has-text("Добавить запись")');
              await page.waitForTimeout(2000);
              
              // Кликаем на dropdown типа - ищем div/span с текстом "A" внутри формы
              console.log('📝 Кликаю на dropdown типа...');
              
              // Это кастомный select, нужно кликнуть на контейнер
              // Ищем по aria-label или по структуре
              await page.click('div[class*="select"]:has-text("A"), span:has-text("A") >> nth=0');
              await page.waitForTimeout(1000);
              await page.screenshot({ path: '01_dropdown_open.png', fullPage: true });
              
              // Выбираем CNAME
              console.log('📝 Выбираю CNAME...');
              await page.click('text=CNAME >> nth=0');
              await page.waitForTimeout(1000);
              await page.screenshot({ path: '02_cname_selected.png', fullPage: true });
              
              // Заполняем хост (portal)
              console.log('📝 Ввожу portal...');
              // Поле хоста - первый текстовый input
              const hostInput = await page.locator('input').filter({ hasText: '' }).first();
              await page.fill('input >> nth=1', 'portal');
              await page.waitForTimeout(500);
              
              // Заполняем target (cname.vercel-dns.com)
              console.log('📝 Ввожу cname.vercel-dns.com...');
              // После выбора CNAME должно появиться поле target
              await page.fill('input >> nth=2', 'cname.vercel-dns.com');
              await page.waitForTimeout(500);
              await page.screenshot({ path: '03_filled.png', fullPage: true });
              
              // Сохраняем
              console.log('💾 Сохраняю...');
              await page.click('button:has-text("Сохранить")');
              await page.waitForTimeout(3000);
              await page.screenshot({ path: '04_result.png', fullPage: true });
              
              // Проверяем
              const newContent = await page.content();
              if (newContent.includes('portal')) {
                console.log('✅ УСПЕХ! Запись portal добавлена!');
              } else {
                console.log('⚠️ Проверь скриншоты');
              }
              
            } catch (e) {
              console.error('❌ Ошибка:', e.message);
              await page.screenshot({ path: 'error.png', fullPage: true });
            }
            
            await browser.close();
          })();
          SCRIPT
          
          node setup_dns.js
      
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: "*.png"
